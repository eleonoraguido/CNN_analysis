# File only used to define some variables that may need to be changed, 
# but not as often as the ones in the config.json file
PDF_SAVE_PATH = "../CNN_analysis_output/" # define where all the output files will be created (model + pdf files). 
LOSS_FUNCTION = "bce"    #loss function used to compile the model
OPTIMIZER = "adam"       # optimizer used to compile the model

